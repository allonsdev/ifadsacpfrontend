import { Component } from '@angular/core';
import { environment } from '../../environments/environment';

const apiUrl = environment.apiUrl;
@Component({
  selector: 'datatable',
  templateUrl: './datatable.element.html'
})
export class DatatableElement {

}
